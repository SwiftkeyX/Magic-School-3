namespace MagicSchool.Contracts
{
    // FIXLATER: this isn't enough dmg kind, I am sure there is more to it, let look at it later. 
    // e.g. passive skill. 
    public enum DamageKindEnum
    {
        AutoAttack = 0,
        Skill = 1,
    }
}
