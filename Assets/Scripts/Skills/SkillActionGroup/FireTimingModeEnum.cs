namespace MagicSchool.Skills
{
    // AtOnce = every shot spawns the same frame, e.g. Ashe's 8 arrows.
    // Sequence = shots spawn one at a time, Interval apart, e.g. Akshan's 6 shots.
    public enum FireTimingModeEnum { AtOnce, Sequence }
}
