namespace MagicSchool.Skills
{
    // AtOnce = every shot spawns the same frame, e.g. a volley of 8 arrows loosed together.
    // Sequence = shots spawn one at a time, Interval apart, e.g. Reyn's 6 shots.
    public enum FireTimingModeEnum { AtOnce, Sequence }
}
